# InputNumber Not-A-Number Class Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an `is-not-a-number` root state class to `AInputNumber` for non-empty invalid current values.

**Architecture:** Reuse the existing decimal validity helper so state-class behavior matches the ARIA invalid-value guard. Keep the new logic as a small computed used only by the root class object.

**Tech Stack:** Vue 3 `<script setup>`, TypeScript, Vitest with Vue Test Utils, VitePress docs.

---

### Task 1: Add RED Coverage

**Files:**
- Modify: `packages/components/src/input-number/__tests__/input-number.test.ts`

- [x] **Step 1: Write the failing test**

Add this test near the existing invalid ARIA value test:

```ts
it('exposes a not-a-number root state class for invalid current values', () => {
  const invalid = mount(InputNumber, {
    props: { modelValue: 'abc' }
  })
  const empty = mount(InputNumber, {
    props: { modelValue: '', stringMode: true }
  })
  const validString = mount(InputNumber, {
    props: {
      modelValue: '1.000000000000000001',
      stringMode: true
    }
  })

  expect(invalid.classes()).toContain('is-not-a-number')
  expect(empty.classes()).not.toContain('is-not-a-number')
  expect(validString.classes()).not.toContain('is-not-a-number')
})
```

- [x] **Step 2: Run the focused test and verify RED**

Run:

```bash
CI=true corepack pnpm --filter ./packages/components exec vitest run --environment jsdom src/input-number/__tests__/input-number.test.ts
```

Expected: the new test fails because the root does not yet include `is-not-a-number`.

### Task 2: Implement Not-A-Number Root Class

**Files:**
- Modify: `packages/components/src/input-number/input-number.vue`

- [x] **Step 1: Add `isNotANumber` computed**

Add this computed near `ariaValueNow`:

```ts
const isNotANumber = computed(() => {
  if (mergedValue.value === undefined || mergedValue.value === '') {
    return false
  }

  return !isValidValueString(String(mergedValue.value))
})
```

- [x] **Step 2: Add `is-not-a-number` to the root class object**

Update `inputNumberClass` so the state object includes:

```ts
'is-not-a-number': isNotANumber.value,
```

- [x] **Step 3: Run the focused test and verify GREEN**

Run:

```bash
CI=true corepack pnpm --filter ./packages/components exec vitest run --environment jsdom src/input-number/__tests__/input-number.test.ts
```

Expected: all InputNumber tests pass.

### Task 3: Update Docs and Generated Outputs

**Files:**
- Modify: `docs/components/input-number.md`
- Generated by build: `packages/components/es/input-number/input-number.vue.js`
- Generated by build: `packages/components/lib/input-number/input-number.vue.js`

- [x] **Step 1: Document not-a-number root state**

Update the native attribute/semantic behavior note to mention the root exposes `is-not-a-number` for non-empty invalid current values.

- [x] **Step 2: Run full verification**

Run:

```bash
corepack pnpm --filter ./packages/components typecheck
CI=true corepack pnpm --filter ./packages/components test
corepack pnpm --filter ./packages/components build
corepack pnpm docs:build
git diff --check
```

Expected: all commands pass.

### Task 4: Commit and Merge

**Files:**
- Stage only files changed for this phase.

- [ ] **Step 1: Commit**

```bash
git add docs/components/input-number.md \
  docs/superpowers/plans/2026-07-06-ant-style-input-number-not-a-number-class.md \
  docs/superpowers/specs/2026-07-06-ant-style-input-number-not-a-number-class-design.md \
  packages/components/src/input-number/__tests__/input-number.test.ts \
  packages/components/src/input-number/input-number.vue \
  packages/components/es/input-number/input-number.vue.js \
  packages/components/lib/input-number/input-number.vue.js
git commit -m "feat: align input number not-a-number class"
```

- [ ] **Step 2: Push and fast-forward `master`**

Push the feature branch, fast-forward `master`, push `master`, then switch back to the feature branch.
