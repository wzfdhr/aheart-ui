# Ant-style InputNumber AutoFocus Plan

## Phase

InputNumber declarative initial focus compatibility.

## Steps

- [x] Add a failing test for `autoFocus` focusing the inner input on mount.
- [x] Add `autoFocus` to InputNumber props.
- [x] Focus the internal input on mount when `autoFocus` is true.
- [x] Document `autoFocus` in the InputNumber API table.
- [x] Regenerate package outputs.
- [x] Run focused and full verification.
- [ ] Commit, push the feature branch, fast-forward merge into `master`, and push `master`.
