# Ant-style InputNumber Mode Plan

## Phase

InputNumber mode compatibility.

## Steps

- [x] Add failing tests for spinner mode class/default icons and custom icon override.
- [x] Add `InputNumberMode` type and `mode` prop.
- [x] Add root mode class with default `input`.
- [x] Switch default controls to `+` / `-` in `spinner` mode while preserving custom controls.
- [x] Add spinner mode CSS for horizontal controls and centered input.
- [x] Document `mode` in the InputNumber API table.
- [x] Regenerate package outputs.
- [x] Run focused and full verification.
- [ ] Commit, push the feature branch, fast-forward merge into `master`, and push `master`.
