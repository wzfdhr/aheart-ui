# D0–D3 final product-manager acceptance

Candidate: `619fbe6`.

## Verdict

Product P0/P1/P2=`0/0/0` for the D0–D3 optimization scope. The candidate is ready for its own PR/CI/merge/Pages delivery gate.

## Accepted user outcomes

- Modal opens, Escape closes it in WebKit repeatedly, and focus returns to the originating trigger.
- SortableList consumer typing remains available through real package entrypoints.
- State helpers, overlay stacks, focus restoration, form dependencies, server errors, reset and async invalidation retain their reviewed behavior.
- Error feedback is readable in desktop/mobile layouts and remains usable at 200% zoom.
- The default danger token no longer creates an ordinary-text contrast failure.

## Remaining boundary

This acceptance closes D0–D3 final review only. D9 root-entry tree shaking, coverage, CI architecture, skip ledger, physical iOS evidence and npm publication remain open; aheart-ui v2 remains paused.
