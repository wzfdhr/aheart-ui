# D4 C批 Select虚拟化产品验收

裁定：接受用户已批准的仅Select静态TanStack接入，关闭virtual搜索空结果高度被压缩的产品P2。本裁定允许进入D4交付清单核对与Draft PR留存，不直接授权以部分功能冒充原路线图全量完成或跳过远端CI合并。

代码56debe7，122文件指纹`1f7b2f227d72235319f21c08fd308648eac447f7edcbb6d761f621394f505bd9`。产品经理实际运行候选脚本复算一致，读取独立开发复审及空状态续审、独立最终测试报告，核对449通过/127既有平台跳过的最终日志，并实际查看最终桌面尾项和窄屏空状态截图。未声称本角色重跑技术测试或独立设计代理审核。

接受范围：virtual默认false；显式true或小配置对象开启；height288、estimateSize32、overscan3及数值回退；真实动态高度、active可访问性、键盘定位、空状态及搜索恢复、SSR/hydration和iframe行为。仅Select正式接入，其他组件虚拟化不因此自动获批。

独立最终测试证据：components1121、dnd44、ai66、scripts86，三包types、确定性双构建、pack971/71/111；完整E2E449通过/127既有平台跳过/0失败；真实消费者36场景、4组hydration和CJS验证通过。初次并发重建使preview崩溃的失败保留为历史，最终冻结独占验证才是本裁定依据。

默认false真实消费者JS gzip60759字节，对照旧A消费者52319字节，增量8440字节约8.24KiB；此为本夹具生产对照，不是任意应用固定增量。Button-only未包含引擎。用户已接受默认关闭仍有静态依赖成本。性能结果含Playwright与帧等待，不宣称纯CPU、SLA或移动App性能。

产品定位：桌面Web专业工具为主要场景；手机浏览网站为辅助响应式兼容。本批已执行的浏览器矩阵保留，不因定位说明删减既有测试；后续不额外引入原生移动App要求。

本批指定范围未发现未关闭产品P1/P2。完整D4应逐条映射原完成清单与实际授权，不将虚拟Tree/TreeSelect/Cascader默认为完成，也不得改写为用户已批准延期。状态/spec中的待批准历史需明确标为历史，当前摘要只保留有效状态。后续代码变化须重验影响范围，远端PR CI及合并后主线/Pages验证仍未完成。
