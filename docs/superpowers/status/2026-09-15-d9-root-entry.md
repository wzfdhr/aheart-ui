# D9 root-entry gate status

The root named-import architecture spike is GREEN on the current branch. The original RED retained unrelated component markers; the final real tgz consumer passes with no workspace symlink, root ESM/CJS/types, Vite build and zero unrelated markers. The Form subpath consumer remains green. The D9 CI split is merged in PR #29, with coverage thresholds and five dedicated QG5 browser jobs passing on its exact head.

The 38 static E2E skip sites are now registered in [the skip ledger](../evidence/d9-skip-ledger.json) and validated by `scripts/qg5-skip-ledger.test.mjs`. A real three-tarball consumer is wired into CI and passes locally; the workflow change and the WebKit timing repair are pending a fresh exact-head PR.

Open D9 gates: fresh PR/master/Pages verification for the browser repair and three-tarball consumer, physical iOS Safari evidence, four-role final D9 audit and npm publication/registry install. v2 remains paused.
